import 'package:flutter/material.dart';

const Color primary = Color(0xFF9C27B0); // Cor roxa utilizada nos campos
const Color textPrimary = Colors.grey; // Cor do texto do label
const Color iconColor = Color(0xFF9C27B0); // Cor dos ícones
const Color buttonColor = Color(0xFFAB47BC); // Cor do botão (apenas para referência futura)
const Color backgroundColor = Colors.white; // Cor do fundo da tela
const Color linkColor = Color(0xFF673AB7); // Cor de links, como "Forget Password"
const Color heading = Color.fromRGBO(71, 26, 160, 100);
pw